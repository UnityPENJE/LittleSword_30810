
namespace LittleSword.Interfaces
{
    // ============================================================
    // IDamageable: 데미지를 받을 수 있는 객체의 인터페이스
    // ============================================================
    // 게임 안에서 플레이어, 적, 보스 등 여러 종류의 오브젝트가 있어.
    // 이들 모두 "피해를 입을 수 있다"는 공통점이 있지?
    //
    // IDamageable을 구현한 오브젝트라면 누구든지 TakeDamage()를 호출할 수 있어.
    // 예를 들어 화살이 맞은 오브젝트가 적인지 플레이어인지 몰라도,
    // IDamageable인지만 확인하면 피해를 줄 수 있어! (다형성 활용)
    // ============================================================
    public interface IDamageable
    {
        // 현재 이 오브젝트가 죽었는지 여부 (HP가 0 이하면 true)
        bool IsDead { get; }

        // 현재 HP 값
        int CurrentHP { get; }

        // 피해를 받는 함수 - damage만큼 HP를 깎아
        // 기본 구현(빈 몸통)을 제공하므로 구현 안 해도 컴파일 오류 안 남
        void TakeDamage(int damage)
        {
            // 각 클래스에서 override해서 실제 피해 처리를 구현해
        }

        // 사망 처리 함수 - HP가 0이 됐을 때 호출
        void Die()
        {
            // 각 클래스에서 override해서 실제 사망 처리를 구현해
        }
    }
}
